# automatically generated by chef cookbook opsmatic-cookbook
# This template is used to enable file-integrity-monitoring https://opsmatic.com/app/docs/file-integrity-monitoring

{
    "files": [
    <% if node["opsmatic"]["file-monitor-list"] && !node["opsmatic"]["file-monitor-list"].empty? %>
        <% node["opsmatic"]["file-monitor-list"].each_with_index do |filepath, index| %>
        {
        "path": "<%= filepath %>"
        <% if index == node["opsmatic"]["file-monitor-list"].size - 1 %>
        }
        <% else %>
        },
    <% end %>
    <% end %>
    <% end %>
    ]
}
